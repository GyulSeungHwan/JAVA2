package yedam.com.variable;

public class Javavariable {
	public static void main(String[] args) {
		//v1 변수 생성 후 15의 값을 초기화.
		int v1 = 15;
		
		if(v1>10) {
			int v2 = v1 - 10;
		}
//		int v3 = v1 + v2; // 빨간색 줄이 쳐져있다면 오류일 것이다
		int v4;
//		v1 = v1 + v4;
}
//v1 = 10; // main 에서 벗어났기 때문에 오류가 난다
}